最新位姿迁移与无独立 KDL 依赖的验证见 [NO_KDL.md](NO_KDL.md)。以下保留历史记录。

# 检查与验证记录

2026-09-22 已将运动学切换为 PIN-IK/Pinocchio，最新结果见 [PIN-IK 替换验证](PIN_IK.md)。

后续发现并修复了实时节拍加速与相机缺少交互的问题，最新结果见
[窗口与节拍验证](VIEWER_FIX.md)；下文保留首次检查记录。
第三方依赖现已改为源码集成，最新构建与回归结果见 [源码集成验证](DEPENDENCY_SOURCES.md)。

日期：2026-09-21。环境：Ubuntu 22.04 x86_64，GCC 11，Release。
未连接、未尝试连接真机；带 SDK 的后端选择测试使用空 IP，在联网前被拒绝。

## 实现结果

- 一个 `aviator` 入口、一个命令处理器和演示序列，YAML 选择 MuJoCo / Rokae。
- 共用 `Aviator.cpp`、TRAC-IK、Pinocchio 碰撞检查和关节轨迹执行。
- 项目内离线第三方依赖包、源码、版本/校验清单与版权说明；配置和模型使用本地路径。
- 按用户要求，真机轮盘开环：双臂实测状态参与跟踪、对准和超时判断；
  轮盘参考随关节轨迹更新，软件 lock/unlock 不依赖外部反馈，状态标记 `open_loop`。
- SDK 静态库隔离在独立 DSO，消除其内嵌 KDL 与运动学库的符号冲突。

## 检查中修复的问题

原版本默认构建在 SDK version script 处失败；后端工厂的 MuJoCo 符号也没有被正确纳入控制库。
现已完整链接并用 `LD_BIND_NOW=1` 检查启动，未使用允许未解析符号的链接选项。

修复了模型先于物理线程释放、无头锁步消费后漏唤醒、渲染等待运动锁、状态字符串并发访问、
SDK TCP 有效标记漏更新、使能沿用旧关节目标、右臂使能失败未回滚左臂、错误固定链遍历轮盘可动关节、
真机反馈伪心跳、按查询次数累计稳定窗口、非法规划周期、故障状态无法解锁等问题。
演示异常现在返回非零退出码。运动中 `status/stop` 实际由独立命令线程处理。

## 测试结果

| 验证 | 结果 |
|---|---|
| 主构建 CTest | **7/7 通过**，73.55 s |
| 基础模块、home 与 J2 姿态 | 通过 |
| 接近 → 锁定 → ±50° → 回中 → 推拉 170 mm → 回中 → 解锁 → 失能 | 通过 |
| 未使能/未对准/已锁定时非法目标的拒绝 | 通过 |
| 配置后端分发、空 IP 在联网前拒绝 | 通过 |
| 开环软件阶段、参考值、时间窗口、反馈超时/失稳 | 通过 |
| 双臂使能失败回滚、零规划周期拒绝 | 通过 |
| 无头交互运动中 status / stop / quit | 通过 |
| GLFW 实际窗口：运动中 status / stop、正常退出 | 通过 |
| 整个源码复制至 `/tmp/aviator-simple-standalone` 独立构建 | 通过 |
| 真机专用构建，`AVIATOR_WITH_SIMULATION=OFF` | 编译和 2 项离线测试通过，ELF 依赖无 MuJoCo/GLFW |
| 仿真专用构建，`AVIATOR_WITH_ROKAE=OFF` | 编译和 3 项针对性测试通过 |
| 安装至独立目录，从 `/tmp` 清除 `LD_LIBRARY_PATH` 后运行完整演示 | 通过 |
| 安装后动态库解析 | 除系统 C/C++ 运行库外均来自安装目录，无兄弟项目依赖 |
| SDK 隔离库动态符号表 | 未导出内嵌 KDL/Model 符号 |

完整输出：[CTest 摘要](ctest.txt)、[验收详细输出](ctest-details.txt)、
[真机专用构建测试](real-build-tests.txt)、[仿真专用构建测试](simulation-build-tests.txt)、
[窗口交互](viewer.txt)、[安装后演示](installed-demo.txt)。

最后仅调整了控制 URDF 的网格资源路径及测试提示文字，额外重跑基础和后端选择测试，
确保本地资源路径可用；几何、关节参数与控制算法未变。

## 仿真实测

| 项目 | 实测 | 判定阈值 |
|---|---|---|
| 接近后 TCP 位置误差，左 / 右 | 1.495409 / 1.495088 mm | < 2 mm |
| 接近后 TCP 姿态误差，左 / 右 | 0.000010 / 0.000009 rad | < 0.01 rad |
| 五次运动结束的最大轮盘转角误差 | 约 0.000001 rad（日志精度） | < 0.005 rad |
| 五次运动结束的最大推拉误差 | 0.000724 mm | < 1 mm |
| 两臂每个物理步的 J2 范围 | 85.498974°～94.500043° | [85°, 95°] |

轮盘验收值读取 MuJoCo 被动关节的实际位置；J2 范围在物理线程每一步更新，
不以命令值或动作终点采样代替全程检查。数值仅说明当前仿真模型中的表现。

## 验证边界

真机 SDK 接口和编译链接已检查，但网络实时性、实机轴映射、工具负载标定、安装误差、
实际握持及运动效果没有硬件验证。轮盘开环初值假定回中，软件 locked 不表示抓取传感器反馈。
沿用原项目的碰撞圆柱掩码，其检测范围见 README；此仿真验收不构成真机碰撞安全证明。

后续速度接口、ServoWheel 和全程关节阻抗修改的最新验证见 [WHEEL_CONTROL.md](WHEEL_CONTROL.md)。

入口与测试简化后的最新回归见 [SIMPLE_TESTS.md](SIMPLE_TESTS.md)。
