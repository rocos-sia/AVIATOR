# Copyright 2025-2026 Inria

cmake_minimum_required(VERSION 3.22)

# Generated by CMake 3.22.1 for jrl-cmakemodules

####### Expanded from @PACKAGE_INIT@ by configure_package_config_file() #######
####### Any changes to this file will be overwritten by the next CMake run ####
####### The input file was config-components.cmake.in                            ########

get_filename_component(PACKAGE_PREFIX_DIR "${CMAKE_CURRENT_LIST_DIR}/../../../" ABSOLUTE)

####################################################################################
message(DEBUG "Welcome to jrl-cmakemodules !")

# valid components that can be searched for
set(jrl-cmakemodules_supported_components jrl-cmakemodules)
message(DEBUG "   Valid components for jrl-cmakemodules: ${jrl-cmakemodules_supported_components}")
message(DEBUG "   User is requesting components: ${jrl-cmakemodules_FIND_COMPONENTS}")

# if no components were requested, add them all.
if(jrl-cmakemodules_FIND_COMPONENTS STREQUAL "")
    message(DEBUG "   No components were requested, including all supported components.")
    set(jrl-cmakemodules_FIND_COMPONENTS ${jrl-cmakemodules_supported_components})
endif()

# iterate through components attempting to be found
foreach(component ${jrl-cmakemodules_FIND_COMPONENTS})
    message(DEBUG "       Adding component ${component}")

    if(${component} IN_LIST jrl-cmakemodules_supported_components)
        message(DEBUG "           Including ${CMAKE_CURRENT_LIST_DIR}/${component}/targets.cmake")
        include("${CMAKE_CURRENT_LIST_DIR}/${component}/targets.cmake")

        message(
            DEBUG
            "           Including ${CMAKE_CURRENT_LIST_DIR}/${component}/dependencies.cmake"
        )
        include("${CMAKE_CURRENT_LIST_DIR}/${component}/dependencies.cmake")

        set(jrl-cmakemodules_${component}_FOUND True)
    else()
        set(jrl-cmakemodules_FOUND False)
        string(
            REPLACE ";"
            ", "
            jrl-cmakemodules_components_pp
            "${jrl-cmakemodules_supported_components}"
        )
        set(jrl-cmakemodules_NOT_FOUND_MESSAGE
            "Unsupported component '${component}'.\n Supported components are:\n ${jrl-cmakemodules_components_pp}"
        )
        break()
    endif()
endforeach()

include("${CMAKE_CURRENT_LIST_DIR}/v2/modules/jrl.cmake")
jrl_print_banner()
    
