# t6 MuJoCo 正弦闭环：1.5 rad/s

测试日期：2026-09-24。原训练 `actor_160k.onnx`、`manifold_phi` LUT、MuJoCo 实测
方向盘与 14 关节反馈。轨迹中心约 `(0 rad,-0.08 m)`，两轴同相正弦，幅值
`(0.02 rad,0.003 m)`，频率 `0.1 Hz`，采样间隔 `10 ms`，持续 `4 s`。
`t6` 关节速度检查设为 `1.5 rad/s`，与训练环境的终止门槛一致。

| 指标 | 结果 |
|---|---:|
| 反馈样本 / 关节命令 | 401 / 400 |
| `t6` 状态 | 401 次 `ok`，0 次拒绝 |
| 原始 RL 动作被缩小 | 0 / 400 次 |
| `theta` 跟踪 RMS / 峰值 | 0.00157308 / 0.00470784 rad |
| `s` 跟踪 RMS / 峰值 | 0.00009268 / 0.000187382 m |
| 关节跟踪 RMS / 峰值（14 轴最大误差） | 0.0057863 / 0.0126964 rad |
| 相位反查每臂 RMS 残差峰值 | 0.00174179 rad |
| LUT 间隙最小值 | 0.00839058 m |
| 接受命令的最大预测关节速度 | 1.416843 rad/s |
| 左 / 右抓握位置误差峰值 | 0.000722 / 0.000675 m |
| `t6` 计算耗时均值 / P99 / 峰值 | 0.125832 / 0.326451 / 0.387775 ms |

所有 400 个原始动作直接通过 `1.5 rad/s` 检查，测试正常结束。与此前
[0.7 rad/s 运行](mujoco_sine_4s.md)相比，本次没有动作缩小，观测到更大的
任务及关节跟踪误差；两次为独立运行，这个对比用于观察趋势，不能单独证明
误差变化的因果来源。原始时序见 [CSV](mujoco_sine_4s_1p5.csv)，终端记录见
[日志](mujoco_sine_4s_1p5.log)。这是 MuJoCo 闭环结果，不代表实体机器人精度。
